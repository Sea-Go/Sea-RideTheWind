# Wiki FactSet 同父前缀的 DataCenter 已确认来源回查

2026-09-16 的本机 test-only Holder 固定 RTW `d10d1d3449dd72bc225061fd1e9bb4100810b8a1`、DC `ebea5f8dfbb0a600b3faca90c235f2ad4a8417b5`、BTW `b708aa463eb68766fb7d20849f475ecbd3e14f27`。RTW 在独立 PostgreSQL 16 中冻结一个两条 required Fact 的目录和三笔单 Fact 人工判定（其中一笔是另一个基线 Wiki）；RTW Outbox 将同一 `ridethewind.knowledge` producer 的 14 条事件送入真实 DC `cmd/platform`。BTW 同一质量 ODS consumer 在自己的临时 PG 中提交 Catalog 1、Judgment 3、技术跳过 10，随后 ACK 14。Holder **在 ACK 后**经 DC service-token 真 HTTP 读取固定 `cutoff=14` 的 `/v1/event-consumers/btw-warehouse-wiki-quality/acknowledged-prefix`，逐个核 offset 1..14、DC 原 Event、DC typed `input_hash`、RTW 四份原 Event JCS、批次 ACK 收据覆盖和批 hash。

`input_hash` 是 DC 对 typed Event 的规范 JSON 哈希；RTW `event_raw_sha256` 是原传输字节哈希，两者不是同一个证据字段。RTW 的原始 Event JCS SHA 才与 DC `input_hash`逐条核对。报告还记录两个目标 Fact 质量 Event offset 严格晚于 Catalog，另一基线质量 Event offset 严格早于 Catalog，目标 FactSet 历史 Revision/SourceScope、两个 Wiki 编辑 head 与人工发布 Release 前后均不变。DC 只读 GET 不创建 consumer、不移动 cursor；这个本地测试未引入产品接口或生产权限路径。

复验命令需显式同时设置 `SEA_BTW_WIKI_FACT_SET_CONSUMER_ROOT=<BTW开发树绝对路径>`、`SEA_DC_EVENT_PLATFORM_ROOT=<DC开发树绝对路径>`、`KNOWLEDGE_KEEP_EVIDENCE=1`、`KNOWLEDGE_PG_BIN=/opt/homebrew/opt/postgresql@16/bin`、`KNOWLEDGE_TEST_FILTER='^TestRealHTTPKnowledgeWorkflow$'`，运行 `bash service/knowledge/scripts/acceptance.sh`。本轮该脚本退出 0，`TestRealHTTPKnowledgeWorkflow` PASS；其余包仅按 filter 编译/race 与全知识包 vet，不称为全包业务测试。独立纯 fixture 的 DC 缺 ACK、错 typed hash、改批收据、offset 缺口、RTW 签 JCS 不符及 `prefix_verified=false` 反例，另经聚焦 race 与 api vet 通过。

隔离证据目录 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-knowledge-acceptance.6ZE5xH`：`test.log` SHA-256 `f0cb12a7664a0180a38d6a19064f4d710349eb864727a38db98cd42be24445c4`，0600 报告 `observability/wiki-fact-set-cross-source.json` SHA-256 `f42d27bf6258c38e7a238003f3475ab00db31f68817da832e0cfe90d369f2b86`，知识服务结构 JSONL SHA-256 `ef72f0210818a5f211d2eea5f8e8692b6f41da2ae16933e09fa0660d501fb9bf`，PG stop SHA-256 `ca19178a35ab4153b75b494963b66ce8243e1b94c173107c87b44d09db23652d`、`pg_ctl status=3`。报告 schema `sea.wiki.fact-set-cross-source.v2` 含 `dc_full_prefix_verified=true`、`dc_ack_cutoff=14`、`dc_acknowledged_at_least=14`、`dc_delivery_batch_count=1`、`dc_prefix_index_jcs_sha256=51e0491eef1c30bbf68694ed18e9c4eb73b94194ba5614ea07d5c04304e80c08`。

这个 L3 证明本机 RTW 原来源到 DC 已 ACK 全前缀再到 BTW ODS 的效果链，不证明管理员的 `facts_complete=true` 客观上列全事实，不构成 Wiki D07 真人质量样本、正式数仓聚合或生产验收；报告分别保留 `human_catalog_verified=false`、`d07_evaluable=false`、`production_verified=false`。
