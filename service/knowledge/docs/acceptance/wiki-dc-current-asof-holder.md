# Wiki 质量来源 V 与 DC ACK 截止 C 的同父验收

状态：RTW **test-only 独立叶子**，从知识开发头 `a07cd671b19c8af2c022042cf97cecc3c5932505` 出发，只接已有 Worker SourceVersion 私有读口和 BTW 新 as-of Reader，不修改 RTW 生产者、Wiki 编辑 head、人工 Release、DataCenter 或 BTW。`SEA_BTW_WIKI_ASOF_READER_ROOT` 是唯一新 opt-in；它必须与原 FactSet Holder 和 SourceProof Reader 指向同一个普通绝对 BTW 树。DWD/另一个消费者/单条 FactSet HTTP fixture 不能与本同游标剧本共选。外部不得直接填 `SEA_BTW_WIKI_ASOF_EVIDENCE_DIR`；RTW 父进程在自己的持久 `observability/wiki-asof-source-witness` 新建空普通 0700 目录后，才传给 BTW 原 `real-source-acceptance.sh`。未选新根时旧 Holder/单机模式继续用原路径和原报告字段。

实际固定源是 RTW 本叶子基 `a07cd67` 的同一份测试代码、DC 开发树 `ebea5f8dfbb0a600b3faca90c235f2ad4a8417b5`、BTW 独立叶子 `da5328d0cb400c13a5ea52406adaddd8a01c4378`。运行前 `df`可用约 2.0 GiB；只启这一套任务专属 RTW/DC 共 PG16 和 BTW 子 PG16，保留数日前遗留的其它 quota PG，不触碰共享或生产数据。先执行纯选择器 `go test -race -p=1 ./service/knowledge/api -run '^(TestWikiFactSetHolder|TestWikiFactSetAsOfHolder|TestWikiFactSetDWDHolder)' -count=1`、`go vet ./service/knowledge/api`、BTW 子脚本 `bash -n` 与 `git diff --check`，全部退出 0。显式源根、SourceProof 与本次持久证据门禁在任何测试 PG 创建前拒绝缺项或混用。

唯一真实父轮设置 `KNOWLEDGE_KEEP_EVIDENCE=1`、`KNOWLEDGE_TEST_FILTER='^TestRealHTTPKnowledgeWorkflow$'`、`KNOWLEDGE_PG_BIN=/opt/homebrew/opt/postgresql@16/bin`，以及 `SEA_BTW_WIKI_FACT_SET_CONSUMER_ROOT=SEA_BTW_SOURCEPROOF_READER_ROOT=SEA_BTW_WIKI_ASOF_READER_ROOT=<BTW da5328d 绝对树>`、`SEA_DC_EVENT_PLATFORM_ROOT=<DC ebea5f8 绝对树>`，运行 `bash service/knowledge/scripts/acceptance.sh`。顶层退出 0；真实 go-zero `TestRealHTTPKnowledgeWorkflow` PASS 15.44 秒，其余知识包按 filter 只做 race 编译和 vet，不称全包业务通过。RTW Worker 新 `POST /internal/v1/knowledge/wiki-quality/source-version-witness/read`在同一个实际子进程先无 Token 401、后 Worker Token 200，结构化 HTTP JSONL 亦有 401 和 200；产品 User JWT/当前单平台 `tenant_id=platform` 兼容槽不参与这个 Worker 来源证据，也没有新增租户设定。

Holder **独立读** RTW 本机模块 `knowledge_modules.event_sequence` 得当前来源版本 `V=14`，再用 Worker POST取得整套已送 `1..V` 候选，核两条 required Fact 的当前 JudgeRevisionID/EventID/raw SHA/JCS SHA。它另从真实 DC Eventing/已 ACK batch 和 BTW 同消费者 ODS **独立读** producer 截止 `C=14`：完整 14 个 offset，目录 1、逐事实质量 3（另页基线 1、目标 2）、明确技术事件 10，BTW 落行后 ACK14、DC 历史 batch 收据 1。此夹具里 `V=C=14` 是两个来源的实测结果；通用 as-of Reader 可以处理不同的 `V` 和 `C`，不机械把 DC offset 当 RTW 模块版本。BTW 原同一 PG 脚本依次实际 PASS `TestRTWRealWikiFactSetSource`、`TestRTWRealFactSetAcknowledgedSource`、`TestRTWRealWikiDCCurrentAlignment`，最后一项只在上述显式 0700 空目录内写两份普通 0600、O_EXCL、无文件尾 LF 的规范 JSON 原工件。

RTW 父 Holder **独立回读并重算**这两份持久原字节：`rtw-source-version-candidate.json` SHA256=`9e1ddcf4edfe1ec40a8fa1549e9a3ba481713e6a0921c52cc89fd7e8c1481299`，`dc-current-source-alignment.json` SHA256=`9055707621fcb6ed109a11b64882d7630847dc5f5a4e72618cac2e1427682dac`。它再把 Candidate canonical 字节与自己先前真实 Worker 200 的 DTO 规范重编字节相等核，把 Receipt 的 `rtw_candidate_jcs_sha256`核同第一份 SHA、`dc_index_sha256`同 RTW 独立 DC 全前缀及 BTW SourceProof 全前缀索引、`ods_evidence_sha256`同 BTW 已 ACK SourceProof 结果；两个当前 JudgeRevisionID/EventID 必须分别同 RTW 目标源判断和 BTW SourceProof 已运输的两条 target Fact 对齐。来源 Source/Wiki 在本读取时可用，Receipt 的 `quality_state=not_evaluable`，`human_catalog_verified=false,d07_evaluable=false,production_verified=false`。

RTW 持久脱敏 `observability/wiki-fact-set-cross-source.json` 是 `sea.wiki.fact-set-cross-source.v5`，SHA256=`2f131f83e5070a0394fbe7669fb9a93f62fffaadf92ca2eaae65b11e135d7cef`；它分别记 `source_asof_version=14`、`dc_ack_cutoff=14`、`source_asof_aligned=true`、上述两原 SHA、`quality_state=not_evaluable`、人工/D07/生产均 false。父 `test.log` SHA256=`fe67f29ea5b3a8394cfa43df7755926c555d837137e34c245f175f6b7879bec1`，结构化 `knowledge-http.jsonl` SHA256=`daad19ad6362acc347ddb333615e152dba21401c371a69cd173fe401b2a2a0bf`，Prom 截面 SHA256=`f8a6dd0af81a7bc84a02a9446028a68670a19d14c0d507ab83bd079e54c46842`；Evidence 目录 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-knowledge-acceptance.nY73Ue`，RTW/DC 共 PG `stop.log` SHA256=`ca19178a35ab4153b75b494963b66ce8243e1b94c173107c87b44d09db23652d`、独立 `pg_ctl status=3`、loopback `51980` 无 LISTEN。

BTW 子目录 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-wiki-sourceproof-real.cdfVVP` 保留三份实际 race 测试日志：ODS SHA256=`e93412f2f78c43891818a9a43adb224692c92e7bf3b8eb993fa96681c26c39b2`、SourceProof SHA256=`7a90ff665874b182a379e19b4a173ac2dca00cd746ca88c2d512712f13f9b944`、as-of SHA256=`f907521da63049bbfa9a984cf864aa800508f5047833751a2a0424f579cb7a5a`；vet 文件为空 SHA256=`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`，`stop.log`同 SHA256=`ca19178a35ab4153b75b494963b66ce8243e1b94c173107c87b44d09db23652d`，`stop-status.log` SHA256=`e138ccb54fdb08283c6eb21159c53207bbbbf07077246e2804e18d22efe6e250`，独立 `pg_ctl status=3`，loopback `52136` 无 LISTEN。旧十一键与十二键结果在 RTW `t.TempDir()` 中按 0600/O_EXCL 回读后随 Go 测试清理，不能再称其原字节持久；**新两份 as-of 文件实际持久**，可从 RTW Evidence 目录重复重算 SHA。

验收只证明这个固定本机夹具的**来源 V 与已 ACK DC/ODS 截止 C 对齐**。管理员的 `facts_complete=true` 是固定 Source 范围的人工声明，不是客观无漏项证明；现有 Wiki Admin JWT 的 `actor_id=test-admin`不能冒作实时 UserCenter UID。真人全事实复核、D07 页达标率、线上 Source Outbox 迁移水位、跨时段撤回后新 C、生产启动与完整产品验收仍未完成。本叶子需由 RTW 开发头逐笔 cherry，再从新 HEAD 做独立第二轮真验收；本记录不借本机一次 PASS 宣称生产。

工作边界：`[W0]` 本独立 RTW 叶子 worktree；`[W1]` `service/knowledge/api` 的 test-only Holder/选择器和本验收文档；`[R1]` DC ebea、BTW da、RTW 知识开发头及 Sea-Docs；`[D1]` 锁定 pgx/go-zero 与 Go 模块缓存；`[G1]` goctl types/routes（本次没有 wire 变化，不重生成）；`[X1]` 两套自启停的 loopback 隔离 PG16；`[N1]` 生产、共享数据库、其它脏工作和用户数据；`[T1]` 上述保留的任务专属 Evidence。主职责 `[C8:VERIFY]`，跨 `[C7:CONTRACT]` Worker SourceVersion/BTW as-of 原工件、`[C4:PERSISTENCE]` DC/ODS ACK 同游标与原 PG 读时 head。
