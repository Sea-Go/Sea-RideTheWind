# H06 实际索引租约过期与替代进程验收

2026-09-15，基于 BTW `f58816d`、RTW `a40adb5` 与 DC `cc73d0af` 的隔离树运行 `service/knowledge/scripts/bge_worker_lease_expiry_acceptance.sh`，最终退出码 0。这个 opt-in 脚本仍使用正式 RTW HTTP/PG、DC `cmd/platform`/PG、锁定的官方 BGE-M3 Provider/Gateway，及 `go build -race ./cmd/worker` 产物启动的准备与索引子进程；模型向量由真实 Gateway 返回，不使用数字夹具。真 User Center 与普通 RTW 两个 HTTP 场景均通过。

准备进程先在 RTW Build fence 1 完成 chunk manifest。旧索引进程从真 DC 领取 **20 秒租约**（attempt 1 / epoch 1），RTW 分配该 Build fence 2；回环代理只将旧进程的第一笔模型表示请求悬置，直到该进程自身的 deadline 取消，并阻断旧 attempt 的失败 CompleteJob。测试从同一个 DC PostgreSQL 读取 `jobs.attempt` 已持久化的 attempt 1 与 `clock_timestamp()>lease_expires_at`，确认实际租约过期；此时旧进程既未得到模型表示也未写 RTW READY/本地 READY/DC ACK。向旧进程发 SIGTERM 后释放被悬置的请求，让替代索引进程以同一持久内容账本和工件重新领取：DC attempt 2 / epoch 2、RTW Build fence 3，官方 BGE 三路表示共 9 次，本地三路 exact Build/Probe、RTW READY、DC 成功回执和 Outbox delivered 同 SHA256。DC attempts 表保留 2 行且仅替代 attempt 有终态结果。再次用旧 attempt、旧 RTW fence 对**替代进程的有效同一索引 Ref**调用 RTW `AcceptBuild(READY)` 和 DC `CompleteJob`，两方均拒绝，成功收据保持不变；RTW 人工发布指针仍未移动。

最终主日志：`/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.o9GXs3/rtw-btw-jobs-test.log`。两个 BTW race/PG/进程报告分别为 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-btw-worker-acceptance.XVXBed/evidence/real-bge-process-report.json`、`/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-btw-worker-acceptance.KJVfWr/evidence/real-bge-process-report.json`，索引 manifest SHA256 分别为 `9303772510a0bf6992faf382ca2d3286d984e50048c7bc1654336d12e714e08e`、`6541fffe3283966558c817f80fad56111e63775abe7bc99275800a10b6beda3a`。各报告记录 `dc_pg_lease_expired=true`、旧 job 状态 `running`、旧 RTW READY 与旧 DC Complete 均拒收，另有旧模型响应 0、新响应 9、DC attempts 2、DC epoch 2、RTW fence 3 和原生 tRPC-Agent-Go prepare/index span；RTW race/PG 在 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-knowledge-acceptance.RwWPLM`。首轮 5 秒 TTL 的业务代码曾走到替代 READY/ACK，但代理挂住的请求妨碍 `httptest.Server.Close`，脚本未正常退出；失败证据 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.oUJEyP`，不计通过。随后用 20 秒真租约和显式释放请求复验成功，没有修改持久租约或使用假时钟。原 ACK 回复丢失场景也在同一改动树回归，两个 RTW 场景均退出 0，日志 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.123yNG/rtw-btw-jobs-test.log`。

等级为 **L3 本机真实租约子链 / H06 PARTIAL**。本轮证明的是**同一 Build generation 1** 下，旧租约/旧 fence 不能接纳 READY/ACK，替代 attempt 可恢复；它不等于改动 Release 后的新 Build generation 验收。旧模型请求在进入 Provider 前被悬置，因此没有覆盖半途完成 Dense 后再恢复 Sparse/Multi-vector 的中间工件复用。DC 模型 Gateway 与 jobs 在两个隔离进程，索引后端是 local-exact、工件是本地共享目录；取消请求、生产对象存储/Milvus、人工发布、发布后查询及规模质量仍待各自门禁。
