# H06 固定 BGE-M3 三路建索引与真实技术任务联验

2026-09-15，隔离 BTW 分支 `feat/h06-bge-real-job-chain-20260915`、RTW 同名分支与 DC `cc73d0af` 做一次 opt-in 联验。运行 `service/knowledge/scripts/bge_job_acceptance.sh`，脚本退出 0；其原始证据目录是 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.qjn4EJ`，RTW 独立 PG/race 目录是 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-knowledge-acceptance.1tMRJJ`，DC BGE 控制面目录是 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-dc-bge.DYOj00`。这些都是本机临时证据，不能作为持久工件或线上凭据。

DC 侧由锁定的 `BAAI/bge-m3@5617a9f61b02` 缓存权重启动真实 Provider，在独立 PG、真实模型配置/CallPoint/Gateway 中验 3 路 document/query 表示；它将仅供本次联验的 gateway 地址、配置 UUID、物理模型和三路 profile 写入权限受限的 runtime 文件。RTW 依据该文件冻结 Release 的 Dense 1024、Sparse 250002、Multi-vector 1024 / `mean_maxsim` profile，并创建未 claim Build。BTW 测试进程从真实 RTW GetRelease/GetRevision/GetBuild 与共享 SHA256 对象读取原文，依次做 Prepare、RTW build epoch 1、真 DC `cmd/platform` index job epoch 1 的 Submit/Claim、RTW build epoch 2 的 Claim、tRPC-Agent-Go IndexWorker/Graph 三路 local-exact Build/Probe、RTW READY、DC CompleteJob 和本地 Outbox delivered。模型表示请求经隔离代理原样进入真实 DC Gateway 和官方 Provider；代理仅将一次性消费者凭证换成任务专属 DC 会话，不生成向量。RTW 父测试直接核对其 PG Build/Outbox 与 DC `jobs.job`/`jobs.attempt`，`READY` 未移动人工发布指针。

联验日志中的两个 `TestRTWRealProviderIndexDispatch`、`TestRealHTTPKnowledgeWorkflowWithUserCenter` 与 `TestRealHTTPKnowledgeWorkflow` 均 PASS；两个独立 Build 的索引 manifest SHA256 分别是 `51413dab10975e86cf523a8c899f35c39fb58befce986ea689ed6be9a1ca4fbf`、`f820b8c4165bdfb27ab8488a1d1d999f0fb7a924b9a2d7c6c0e1f33cbd930a3e`。真实 User Center 进程参与第一个 RTW 场景，RTW/BTW race、两仓 vet 与 diff 检查退出 0。真实 BGE 模型锁、Provider 源文件 SHA 和三路 document/query 响应位于 DC 证据目录。复现须由调用者显式提供三个独立工作树、缓存快照和锁定 serving 目录的绝对路径；脚本只启动并清理本机隔离 PG/回环服务。

验收等级为 **L3 真实本机子链 / H06 PARTIAL**。本轮 Prepare 是正式 `Preparer.Prepare`，Index 是正式 `IndexWorker.RunOnce`，但二者由测试进程装配；并未让 `cmd/worker` 的准备与索引两个常驻进程同次消费真 DC jobs。DC `cmd/platform` 的 jobs 与模型控制面/Gateway 在本轮是两个真实服务进程，不是单一生产部署。索引后端是完整 local-exact 而非 Milvus；RTW READY 未人工发布，用户查询流量、生产对象存储、故障恢复、取消补偿和规模质量不在本验收内。下一交接须补正式双进程 Prepare→Index、同一个 DC 部署面配置、正式对象存储/索引后端以及发布后查询门禁，分别附可复现的任务与工件收据。
