# H06 真 User Center 发布、搜索与回滚

2026-09-15，基于 RTW `26d76d2`、BTW `ca5f439`、DC `b499cd46` 的三个固定隔离工作树运行 `service/knowledge/scripts/bge_worker_publish_search_acceptance.sh`。本轮只选择 `TestRealHTTPKnowledgeWorkflowWithUserCenter`：验收脚本用一次性 PostgreSQL 编译并启动真实 User RPC 与 User Center API，分别注册登录 owner/other 两个账户；RTW 从真实 JWT 和 User RPC 解析 owner SubjectRef。用户服务保持运行，直到旧发布、新发布、回滚三次正式搜索与历史隔离全部完成，随后原有的禁用、删除和 RPC 停止拒绝门禁继续通过。

索引、发布和检索行为与前一轮相同：旧基线必须经官方锁定 BGE-M3/Go 三路 builder、RTW READY 后才由测试管理员激活；旧候选在建时经 DC/RTW 显式取消，新 Release ordinal 3 的 Build generation 1 被 `SUPERSEDED`，generation 2 经正式准备/索引 `cmd/worker` 取得真实 BGE 三路工件并 READY。测试管理员显式发布新代后再回滚旧基线，三个 RTW 权威 SearchSnapshot 分别固定发布修订 1、2、3 和各自 Dense/Sparse/Multi-vector Ref/hash。三次 RTW 产品请求签名的 scope 必须逐字段等于当次权威 Snapshot，SubjectID 必须等于 User Center 本轮注册 UID，SearchID/AnswerID 必须等于返回结果；正式 `cmd/api` 的 Go 检索依次命中 `public`、`south`、`public`。新回答的 RTW 引用表只含新 source revision，owner 历史保留三次回答，other 登录账户不可见它们。

最终字段版脚本退出 0，主日志 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.vA5DGr/rtw-btw-jobs-test.log`；更强的最终报告位于 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-knowledge-acceptance.AW54Ay/observability/h06-publish-search-rollback.json`，SHA-256 为 `49557737abfa9f51dda607a042c082600031650d18d44e3e78d0bdb3f6685d0c`。报告记录 `real_usercenter_login_rpc=true`、`real_usercenter_uid=2099651300798582784`、完整 `subject_ref=rtw.identity/platform/2099651300798582784` 与 `other_subject_history_empty=true`；三次签名 scope 在测试中逐字段等于 RTW 当时的权威 SearchSnapshot，SubjectID 等于该真实注册 UID。旧/新索引 SHA-256 分别为 `d68e72013f237492fd5e536d245a5c2e0e63f779c4ef886a2754d1a254926c60`、`c3633f4d3ad8f8e8f6d13a3c62450a1d982300d600f284b054c9721498c5f633`。BTW 取消/新代进程报告在 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-btw-worker-acceptance.jFQyZe/evidence/cancel-release-report.json`。三份正式 API JSONL 各 8 行，逐行 JSON、服务字段、终态 outcome、SearchID/AnswerID、HTTP/Runtime 同 trace、恰一次启停的 Go 门禁全部通过；另一登录账号用真实 token 读取本人历史，未出现三笔目标 AnswerID。搜索完成后再执行的账号停用、删除和 User RPC 停止门禁也继续通过。

等级为 **L3 本机真 User Center 子链，H06/H02 PARTIAL**。User Center、RTW、DC 和 BTW 都是一次性本机进程/数据库；管理员发布不影响生产。真实模型只覆盖 BGE-M3 检索表示，摘要仍是本机固定 OpenAI 兼容 fixture，不代表 Chat 模型质量或正式用量；旧基线由测试 builder 构建，新代由正式 worker 构建。尚未验证 Milvus、生产对象存储、线上用户、WhaleHall 展示和生产发布。
