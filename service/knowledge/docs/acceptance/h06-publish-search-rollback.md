# H06 新代发布、正式搜索与回滚验收

2026-09-15，隔离 RTW `7ce5ed5`、BTW `90b8569`、DC `b499cd46` 基线上运行 `service/knowledge/scripts/bge_worker_publish_search_acceptance.sh`。测试独占模块的旧基线先从锁定官方 BGE-M3 Provider/Gateway 获得真实 Dense、Sparse、Multi-vector 表示，Go 三路 builder 完成不可变索引及独立 Probe，RTW 接纳 READY 后测试管理员才激活旧发布。旧基线的构建由测试 builder 装配；取消后的新 Release ordinal 3 / Build generation 2 则由正式准备与索引 `cmd/worker` 子进程完成。中间旧候选 ordinal 2 已经 DC/RTW 显式取消且无法 READY/ACK，新的 generation 1 被 RTW 标 `SUPERSEDED`。这些都是独立一次性 PostgreSQL 与回环进程，不涉及生产发布。

在新 Release 发布前，RTW 签名 SearchSnapshot 的发布修订为 1，三路 Ref/hash 全部指向旧 BGE Build；独立 `cmd/api` 的 fast/low 搜索通过真实 Go 检索命中旧 source 的 `public`，RTW 接纳其引用和回答。测试管理员以 CAS 显式激活新 generation 2 后，发布修订 2 与三路 Ref/hash 全部切换到新正式 worker 工件；新的独立 `cmd/api` 对 `south` 做真实检索，只引用新 source revision，RTW 的 `knowledge_answer_citations` 仅有该 revision，属于该用户的 AcceptedAnswer 历史可查而其他 subject 不可见。管理员再以 CAS 回滚到旧 Release/Build，发布修订 3 的三路 Ref/hash 精确恢复到旧索引；第三个独立 `cmd/api` 对 `public` 再次命中旧 revision。三个搜索均经过 RTW 产品 HTTP 签名转发到正式 API，检索候选来自真实索引，不由测试注入。摘要生成仍由本机固定 OpenAI 兼容模型夹具读取实际 EvidencePack，因此本轮不宣称真实 LLM 的摘要质量或 DataCenter Chat 用量。

验收检查三次 RTW SearchSnapshot 的 module/release/build generation、发布修订和每路不可变 Ref；新/旧答案 ID、片段 revision、RTW 引用表、owner 历史与回滚后旧引用均独立比对。正式 `cmd/api` 拒绝未签名请求、保留结构化启动/停止日志、原生 tRPC-Agent-Go `search_summary_root` span、Metrics 与真实模型输入的 EvidencePack。新答案不会因回滚从历史被抹除，历史引用仍保留当时的发布身份；本轮没有假定回滚会撤销已接纳的历史事实。

首次失败是 RTW 管理员 `CreateSource` 回执依合同省略原文，而测试 helper 误将其当冻结内容，证据 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.6VHVNw`；改从私有 `GetRevision` 读取后，第二次失败是将新签名搜索的 `search-facade-session` 历史误查到原场景 `http-session`，证据 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.8Z1Rdj`。两轮均未计通过。最终源码复验脚本退出码 0，主日志 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.9lUdhW/rtw-btw-jobs-test.log`；RTW PG/race 及 0600 报告 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-knowledge-acceptance.z8SgWT/observability/h06-publish-search-rollback.json`，同目录另外保留三份正式 `cmd/api` JSONL。BTW 取消/新代进程 race/PG 报告 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-btw-worker-acceptance.h4w9dq/evidence/cancel-release-report.json`。旧/新索引 manifest SHA256 分别为 `0d2e76317299378e23854e3ba040cb329ac2a11a7df5b23a071aa14b94a010bf`、`655849dc6c460db956fe5ba6f6cd3ee8af5227edd3183f2e666fc89d9b893c62`；报告记录三次发布修订的全部 Dense/Sparse/Multi-vector Ref/hash，旧→新→回滚三路都精确匹配。日志与报告不含真实服务凭据。

等级为 **L3 本机发布/检索/回滚子链，H06/H02 PARTIAL**。只覆盖 fast/low/summary 的本地 exact 后端，模型权重是官方 BGE-M3 固定快照，旧发布构建使用测试 builder，新发布构建使用正式 worker；尚无 Milvus、生产对象存储、真实聊天模型质量、WhaleHall 展示、生产用户流量或上线发布验收。测试的用户身份来自隔离 gRPC fixture，非真实 User Center 场景。
