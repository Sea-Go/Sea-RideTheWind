# H06 两个正式 worker 进程的锁定 BGE-M3 建索引验收

2026-09-15，基于 RTW `f244cdb`、BTW `32b4df2`、DC `cc73d0af` 的隔离工作树，通过 `service/knowledge/scripts/bge_worker_process_acceptance.sh` 做同轮 opt-in 联验，最终脚本退出码 0。它先启动真实锁定 `BAAI/bge-m3@5617a9f61b02` Provider 与 DC 模型配置/Gateway，再运行 RTW 知识 HTTP/PG 和实际 DC `cmd/platform`/PG。RTW 固定 Release 的 Dense 1024、Sparse 250002、Multi-vector 1024 / `mean_maxsim` profile 和未 claim Build；BTW 以 `go build -race ./cmd/worker` 构建一个二进制，再在隔离 PG 中先后运行准备、索引两个**实际子进程**，分别领取 `content.prepare.v1` 和 `content.build.v1`。两个不同 DC job 都从技术 lease epoch 1 开始；RTW 对同一 Build 分配准备 fence 1、索引 fence 2。模型请求通过仅转发 HTTP 与一次性凭证的回环桥接进入真实 DC Gateway/官方 BGE Provider，没有生成夹具向量。

索引进程完成本地三路 exact Build/Probe 与 RTW READY 后，联验代理对其第一笔 DC CompleteJob 返回 503；此时 DC job 仍 `running`、本地 Outbox 未交付。测试向索引进程发 SIGTERM 并确认干净退出，再用同一 worker 配置和持久 PG/工件重启；`IndexWorker.DispatchReadyOnce` 重读 RTW READY、同代 DC attempt、同一 manifest Ref，完成 DC 技术 ACK 与 Outbox delivered。重启前后每个 Build 的真实 BGE 表示调用数均为 9，没有重新编码。两个 RTW 场景（其中一个含真实 User Center）分别通过，索引 manifest SHA256 为 `262b80d7d13f1b4f9251ea42477e374a3e5b4fef5643631fe3a9b45bdc7c4fe4` 和 `5890f03837fd7690110f58cd2d574bc838af965dead0b5f5d583b9162ffeb807`；每个场景仅注入 1 次失败完成请求，OTLP 各收到 tRPC-Agent-Go 原生 prepare/index Agent span 各 1。RTW 父测试还核对其 PG Build/Outbox、DC `jobs.job`/`jobs.attempt` 与相同 Ref，READY 没有移动人工发布指针。

原始主日志和 DC Provider 日志分别在 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.DNKxyC/rtw-btw-jobs-test.log` 与同目录 `dc-bge-test.log`；RTW PG/race 证据在 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-knowledge-acceptance.ZwPBrl`；BTW 两次 race/PG 证据与进程 JSONL/结构化报告分别在 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-btw-worker-acceptance.vo76UB` 和 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-btw-worker-acceptance.iNjbWC`。这些是本机临时证据，不含线上部署结论。

验收等级为**L3 本机进程子链 / H06 PARTIAL**。本轮验证了当前 DC lease 有效期内的 ACK 丢失、SIGTERM、重启补投；未把实际 DC lease 运行到过期，也未验证取消后的新 attempt 补偿。DC job 服务与模型 Gateway 是两个真实隔离进程，不是同一部署单元；工件为共享本地 SHA256 目录，三路后端为 local-exact，尚无生产对象存储/Milvus、人工发布、发布后用户查询和规模质量验收。上述缺口需要独立门禁，不能由进程链成功推导完成。
