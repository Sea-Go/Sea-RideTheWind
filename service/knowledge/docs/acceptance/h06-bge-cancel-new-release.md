# H06 取消在建候选并构建新 Release generation

2026-09-15，基于 BTW `86b62e1`、RTW `cfe0b03`、DC `cc73d0af` 的隔离树运行 `service/knowledge/scripts/bge_worker_cancel_new_release_acceptance.sh`。测试先在独占本机 RTW PostgreSQL 模块创建一份**结构性基线** Release ordinal 1/READY Build，并通过真实管理员 API 激活；这是测试夹具的发布指针，不是用户或生产内容。其后冻结 ordinal 2 的官方 BGE-M3 候选 Release，准备进程生成不可变 chunk。旧索引进程获得真 DC job 与 RTW Build fence 2，第一笔 BGE 模型响应在回环代理处短暂悬置；真实 DC `CancelJob` 使其 `cancel_requested`/cancel_version 1，真实 RTW 管理员 `CancelBuild` 使旧 Build `CANCELLED`。释放已由官方 Provider/Gateway 计算的响应后，旧进程仍有模型调用，但不能产生 READY 或技术成功；其 DC 取消由技术 API 确认并留存无结果的 attempt 行。

在相同模块内，管理员通过真实 HTTP 创建不同内容的 source 和 Release ordinal 3，其 manifest SHA256 与旧候选不同；随后建 Build generation 1 和 generation 2，RTW 将前者标 `SUPERSEDED`。新的正式准备、索引 `cmd/worker` 进程消费 generation 2，官方 BGE-M3 输出构成 Dense、Sparse、Multi-vector 三路 local-exact 工件，三个独立 Probe 通过；RTW READY 与 DC 成功回执绑定同一个新的索引 SHA256。旧 worker 的 attempt/fence 再拿这个有效新 Ref 请求 RTW `AcceptBuild(READY)` 和 DC `CompleteJob`，两方均拒收；旧 Build 保持 `CANCELLED`，旧 DC job 保持 `cancelled`、无 result_hash。基线已发布 Release ID、Build ID、指针修订 1 和 `knowledge_publications` 行数前后完全相同。RTW 数值 fence 在**各自 Build 内**分配；本次证明旧/新 `(build_id, lease_epoch)` 组合独立，不把同为 2 的 RTW epoch 冒充全局递增。

正式验收等级为 **L3 本机进程/PG 子链，H06 PARTIAL**。本轮含真 User Center 和普通 RTW HTTP 两个场景、实际 DC `cmd/platform`/PG、真实官方模型 Provider/Gateway、tRPC-Agent-Go 原生 prepare/index span 与三路工件，但工件仍在共享本地 SHA256 目录、索引后端是 local-exact。新 Release 仅 READY，**没有激活新发布指针**；取消后的外部实时广播、Milvus/生产对象存储、正式发布与用户查询、规模质量仍需独立验收。第一次尝试复用主 HTTP 场景已发布模块时，其来源早已撤回，新增源返回 410，证据 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.fkP1e9`；第二次 BTW 子链完成但父测试在解码结果前比较空 BuildID 而退出 1，证据 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.ow4xKw`。这两轮均不计通过。

最终固定 DC `cc73d0af` 的独立只读工作树复验脚本退出 0，主日志 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-h06-bge-job.cjG2zx/rtw-btw-jobs-test.log`，两个 BTW race/PG、命令进程 JSONL 与结构报告分别在 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-btw-worker-acceptance.nSsfG2/evidence/cancel-release-report.json` 和 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-btw-worker-acceptance.ia8eLA/evidence/cancel-release-report.json`，RTW PG/race 证据目录 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-knowledge-acceptance.XrsIsF`。两个新索引 manifest SHA256 分别是 `6f540c4f83a32d1a596d30669bac76ab26c633f122ef27a8295a3968cd88a8c1` 与 `e70721e88fc3300ffeb1bce37b6e03f93a0cb964a649faf11bf504c81694706e`；报告同时记载旧/新 Release manifest SHA256、不同 chunk manifest、旧 DC/RTW 取消状态、旧复投双拒、发布 Release/Build ID 和修订前后不变。两场景均由真实进程/PG 验收，报告没有令牌或 DSN。
